# Reference screenshot

Put a **full-page screenshot** of your site here as `page.png` (1440px wide works best).
`immersive-film.html` shows it in every "REFERENCE · 제가 만든 페이지" window; without it, an example mockup is shown.

Re-render after adding it:

```bash
node render.cjs 30 immersive.mp4 immersive-film.html
```
